---
import Badge from "@/components/Badge.astro"
import SectionContainer from "@/components/SectionContainer.astro"
import SocialPill from "@/components/SocialPill.astro"
import LinkedInIcon from "@/components/icons/LinkedIn.astro"

import Layout from "@/layouts/Layout.astro"
import { getI18n, getLangFromPath } from "@/i18n"
import type { Lang } from "@/i18n"

const lang: Lang = getLangFromPath(Astro.url)
const text = getI18n(lang).components
---

<Layout title="" description="" lang={lang}>
  <SectionContainer class="py-44 pb-32 flex flex-col gap-y-10">
    <article class="mb-10">
      <h1 class="mb-4 text-xl font-semibold">{text.badgeTitle}</h1>

      <Badge>{text.badgeText}</Badge>
    </article>

    <article class="mb-10">
      <h1 class="mb-4 text-xl font-semibold">{text.pillTitle}</h1>

      <div class="flex">
        <SocialPill href="https://linkedin.com/in/guillermo-teijeiro-7781a232">
          <LinkedInIcon class="size-4 md:size-6" />
          LinkedIn
        </SocialPill>
      </div>
    </article>
  </SectionContainer>
</Layout>
